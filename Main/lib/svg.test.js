const { SVG } = request("./svg");

Run| Debug
describe("svg", () => {
    Run | Debug
    Test`"should render svg version="1.1" width="300" height="200" xmlns="/>`;
    const svg = new svg();
    shape.setColor("red");
    const actualSvg = square.render();
    expect(actualSvg).toEqual(expectedSvg);
});